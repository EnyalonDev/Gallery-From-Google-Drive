# Option 2: React Gallery

A modern component-based implementation using React and Vite.

## 🇺🇸 Technical Description
- **Stack**: React 18, Vite.
- **Features**: Custom Hook `useDriveImages`, CSS Modules (conceptually), Responsive Grid.
- **State**: `useState` and `useEffect` for data fetching.

### How to Run
1.  **Install**:
    ```bash
    npm install
    ```
2.  **Run Dev Server**:
    ```bash
    npm run dev
    ```
3.  **Build (Production)**:
    ```bash
    npm run build
    ```

### Configuration
Click the "⚙️ Config" button on the top right to set your Google Script URL and Folders locally.

---

# Opción 2: Galería en React

Una implementación moderna basada en componentes usando React y Vite.

## 🇪🇸 Descripción Técnica
- **Tecnologías**: React 18, Vite.
- **Características**: Hook personalizado `useDriveImages`, Grid Responsivo.
- **Estado**: Uso de `useState` y `useEffect` para el consumo de la API.

### Cómo Ejecutar
1.  **Instalar dependencias**:
    ```bash
    npm install
    ```
2.  **Correr servidor de desarrollo**:
    ```bash
    npm run dev
    ```
3.  **Construir para producción**:
    ```bash
    npm run build
    ```

### Configuración
Haz clic en el botón "⚙️ Configurar" arriba a la derecha para establecer tu URL del Script de Google y tus carpetas.

---

**Credits / Créditos**: Néstor Ovallos Cañas
- [www.nestorovallos.com](https://www.nestorovallos.com)
- [www.portafoliocreativo.com](https://www.portafoliocreativo.com)
