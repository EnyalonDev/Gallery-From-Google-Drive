# Option 3: Next.js Gallery

A high-performance implementation using Next.js App Router.

## 🇺🇸 Technical Description
- **Stack**: Next.js 14+ (App Router), TypeScript.
- **Features**: Lazy Loading, Optimized Image rendering components, Client-side Configuration.
- **Structure**: `/src/app` for routes, `/src/components` for UI.

### How to Run
1.  **Install**:
    ```bash
    npm install
    ```
2.  **Run Dev Server** (Port 3700 configured):
    ```bash
    npm run dev
    ```
3.  **Build**:
    ```bash
    npm run build
    npm start
    ```

### Configuration
Go to the **⚙️ Configurar Galerías** link or visit `/config` directly to manage your API connections.

---

# Opción 3: Galería en Next.js

Una implementación de alto rendimiento usando Next.js App Router y TypeScript.

## 🇪🇸 Descripción Técnica
- **Tecnologías**: Next.js 14+ (App Router), TypeScript.
- **Características**: Carga perezosa (Lazy Loading), Componentes optimizados, Configuración del lado del cliente.
- **Estructura**: `/src/app` para rutas, `/src/components` para la UI.

### Cómo Ejecutar
1.  **Instalar dependencias**:
    ```bash
    npm install
    ```
2.  **Correr servidor de desarrollo** (Puerto 3700 configurado):
    ```bash
    npm run dev
    ```
3.  **Construir para producción**:
    ```bash
    npm run build
    npm start
    ```

### Configuración
Ve al enlace **⚙️ Configurar Galerías** o visita la ruta `/config` directamente para gestionar tus conexiones y carpetas de Drive.

---

**Credits / Créditos**: Néstor Ovallos Cañas
- [www.nestorovallos.com](https://www.nestorovallos.com)
- [www.portafoliocreativo.com](https://www.portafoliocreativo.com)
