# Option 4: Vue.js Gallery

A clean and modular implementation using Vue 3.

## 🇺🇸 Technical Description
- **Stack**: Vue 3, Vite.
- **Features**: Composition API (`<script setup>`), Reactive State, Scoped CSS.
- **Logic**: Reusable composable `useDriveImages.js`.

### How to Run
1.  **Install**:
    ```bash
    npm install
    ```
2.  **Run Dev Server** (Port 3725 configured):
    ```bash
    npm run dev
    ```
3.  **Build**:
    ```bash
    npm run build
    ```

### Configuration
Use the toggle button "⚙️ Configurar" in the header to switch to the settings view and update your API URL and Folders.

---

# Opción 4: Galería en Vue.js

Una implementación limpia y modular usando Vue 3.

## 🇪🇸 Descripción Técnica
- **Tecnologías**: Vue 3, Vite.
- **Características**: Composition API (`<script setup>`), Estado Reactivo, CSS Scoped.
- **Lógica**: Composable reutilizable `useDriveImages.js`.

### Cómo Ejecutar
1.  **Instalar dependencias**:
    ```bash
    npm install
    ```
2.  **Correr servidor de desarrollo** (Puerto 3725 configurado):
    ```bash
    npm run dev
    ```
3.  **Construir para producción**:
    ```bash
    npm run build
    ```

### Configuración
Usa el botón de alternancia "⚙️ Configurar" en el encabezado para cambiar a la vista de ajustes y actualizar tu URL de la API y carpetas.

---

**Credits / Créditos**: Néstor Ovallos Cañas
- [www.nestorovallos.com](https://www.nestorovallos.com)
- [www.portafoliocreativo.com](https://www.portafoliocreativo.com)
